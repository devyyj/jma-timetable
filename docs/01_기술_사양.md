# JMA Timetable 기술 사양서 (V3)

## 1. 데이터베이스 스키마 (Drizzle ORM)

### `profiles` (회원 정보)
```typescript
export const profiles = pgTable("profiles", {
  id: uuid("id").primaryKey().references(() => authUsers.id),
  fullName: text("full_name").notNull(),
  phone: text("phone"),
  role: text("role", { enum: ["user", "admin"] }).default("user").notNull(),
  provider: text("provider"), // 'kakao'
  createdAt: timestamp("created_at").defaultNow().notNull(),
});
```

### `rooms` (연습실)
관리자 페이지에서 동적으로 CRUD가 가능해야 하므로 `isActive` 및 `sortOrder` 등의 추가 필드 고려.
```typescript
export const rooms = pgTable("rooms", {
  id: uuid("id").primaryKey().defaultRandom(),
  floor: integer("floor").notNull(), 
  roomName: text("room_name").notNull(),
  roomType: text("room_type").notNull(), // 'general', 'drum', 'midi', 'ensemble', 'lesson'
  isActive: boolean("is_active").default(true).notNull(), // 관리자가 비활성화 가능
  sortOrder: integer("sort_order").default(0).notNull(), // 화면 표시 순서
});
```

### `reservations` (예약)
```typescript
export const reservations = pgTable("reservations", {
  id: uuid("id").primaryKey().defaultRandom(),
  userId: uuid("user_id").references(() => profiles.id), // 회원일 경우
  guestName: text("guest_name"), // 비회원일 경우 실명 입력
  password: text("password"), // 비회원 예약 관리용 4자리 숫자
  roomId: uuid("room_id").references(() => rooms.id).notNull(),
  date: date("date").notNull(),
  startTime: integer("start_time").notNull(),
  endTime: integer("end_time").notNull(),
  status: text("status", { enum: ["confirmed", "cancelled", "noshow", "lesson"] }).default("confirmed").notNull(),
  createdAt: timestamp("created_at").defaultNow().notNull(),
});
```

---

## 2. 핵심 예약 로직 (Server Actions)

### `createReservation` 프로세스 (연속 예약 제한 & 55분 연장 규칙)
1. **입력값 검증:** `roomId`, `date`, `startTime`, `guestName`, `password` 유효성 체크.
2. **중복 예약 확인:** 동일 시간대 중복 예약 원천 차단.
3. **연속 시간 검증 (55분 룰):**
   * **제한:** 동일한 `guestName` 또는 `userId`로 앞 시간(`startTime - 1`)이 예약되어 있는 경우 연속 예약으로 간주.
   * **예외 허용:** 현재 시각(KST 기준)이 이용 중인 시간의 **종료 5분 전(55분)** 이후인 경우에만 다음 슬롯 예약 허용.
   * **메시지:** 55분 이전 시도 시 "연속 시간 예약은 이용 중인 시간 종료 5분 전(55분)부터 가능합니다." 안내.

### `cancelReservation` 프로세스 (보안 강화)
1. **비밀번호 검증:** 비회원 예약의 경우, 예약 시 설정한 4자리 `password`가 일치해야만 삭제 가능.
2. **회원 처리:** (추후) 본인의 예약인 경우 세션 기반으로 자동 검증.

---

## 3. UI/UX 사양

### 모바일 최적화 입력
* **이름 입력:** 최대 10자 제한 및 실시간 글자 수 표시.
* **비밀번호 입력:** 
    * PC: `type="password"`로 보안 유지 및 자간(tracking) 최적화.
    * 모바일: `inputMode="numeric"`을 사용하여 숫자 키패드 자동 활성화.
* **푸터:** 간결한 이용 수칙 안내 메시지만 노출.


---

## 3. 인증 설정 (Supabase Auth)

### 소셜 로그인 구성
- **Kakao:** REST API 키 및 Redirect URI 설정. (카카오 단독 지원)

### 권한 제어 (RLS Policy)
- `reservations` 조회: 모든 사용자(비인증 포함) 가능.
- `reservations` 생성: 모든 사용자 가능.
- `reservations` 수정/삭제: 
    - 회원은 `userId`가 일치할 때만 가능.
    - 비회원은 세션/쿠키 등을 통한 `temp_token` 일치 시에만 가능.
- `rooms` 테이블: 
    - 조회: 모든 사용자 가능.
    - 생성/수정/삭제: **`role`이 'admin'인 사용자만 가능.**

---

## 4. 실시간 상태 동기화
- `reservations` 및 `rooms` 테이블에 `INSERT`, `UPDATE`, `DELETE` 발생 시 `supabase.channel`을 통해 클라이언트에 브로드캐스팅.
- 특히 관리자가 방의 상태나 순서를 변경했을 때 클라이언트 UI가 즉각 새로고침되도록 적용.
��했을 때 클라이언트 UI가 즉각 새로고침되도록 적용.
